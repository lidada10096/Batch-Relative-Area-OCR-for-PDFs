### 恢复计划

**目标：** 去除图像纠偏功能，恢复原有代码逻辑，参考备份文件 `paddle_ocr_regions copy.py`

**执行步骤：**

1. **移除图像纠偏相关函数**：
   - 删除 `preprocess_image` 函数
   - 删除 `detect_skew_angle` 函数
   - 删除 `correct_skew` 函数
   - 删除 `show_correction_preview` 函数

2. **恢复参数配置**：
   - 将 `POPPLER_PATH` 恢复为 `None`
   - 移除 `MANUAL_CORES` 配置
   - 移除复杂的 poppler 路径检测逻辑

3. **恢复 `calculate_optimal_workers` 函数**：
   - 移除 `max_allowed_workers` 参数
   - 恢复原有的核心数计算逻辑
   - 移除至少留出1个核心的限制
   - 恢复基于PDF大小的核心数调整逻辑
   - 恢复2-4核的限制范围

4. **恢复 `draw_boxes` 函数**：
   - 移除图像纠偏处理逻辑
   - 保留改进的 `draw_hint_text` 函数（大红色文字、粗体、无背景）

5. **恢复 `ocr_page_worker` 函数**：
   - 移除图像纠偏处理逻辑
   - 移除 `Image.fromarray` 相关代码
   - 恢复原有的OCR处理流程

6. **恢复 `process_single_pdf_multiprocess` 函数**：
   - 移除 `manual_cores` 参数
   - 恢复原有的核心数计算调用
   - 恢复原有的函数签名

7. **恢复主程序入口**：
   - 移除核心选择菜单
   - 恢复为直接使用智能核心数计算
   - 移除 `cores_to_use` 相关代码

**预期结果：**
- 程序不再执行图像纠偏
- 恢复为原有智能核心数计算逻辑
- 移除手动核心选择功能
- 保留改进的画框提示文字样式
- 程序能够正常处理PDF文件

**注意事项：**
- 确保备份文件包含所有必要的功能
- 保留用户体验改进（如大红色提示文字）
- 测试程序能否正常运行